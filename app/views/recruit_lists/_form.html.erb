<%= form_with(model: recruit_list, local: true) do |form| %>
  <% if recruit_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recruit_list.errors.count, "error") %> prohibited this recruit_list from being saved:</h2>

      <ul>
        <% recruit_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
